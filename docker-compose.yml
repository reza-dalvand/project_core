version: '3.9'

services:
  # ═══════════ Database (PostGIS) ═══════════
  postgres:
    image: postgis/postgis:16-3.4-alpine
    container_name: beau_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: beau
      POSTGRES_USER: beau
      POSTGRES_PASSWORD: ${DB_PASSWORD:-beau_secure_password}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U beau"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ═══════════ Redis ═══════════
  redis:
    image: redis:7-alpine
    container_name: beau_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ═══════════ Backend App ═══════════
  backend:
    build:
      context: .
      target: ${DOCKER_TARGET:-development}
    container_name: beau_backend
    restart: unless-stopped
    volumes:
      - .:/app
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DJANGO_SETTINGS_MODULE=${DJANGO_SETTINGS_MODULE:-config.settings.development}
      - DB_ENGINE=django.contrib.gis.db.backends.postgis
      - DB_NAME=beau
      - DB_USER=beau
      - DB_PASSWORD=${DB_PASSWORD:-beau_secure_password}
      - DB_HOST=postgres
      - DB_PORT=5432
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: >
      sh -c "
        python manage.py migrate --noinput &&
        python manage.py create_initial_data || true &&
        python manage.py runserver 0.0.0.0:8000
      "

  # ═══════════ Celery Worker ═══════════
  celery_worker:
    build:
      context: .
      target: ${DOCKER_TARGET:-development}
    container_name: beau_celery_worker
    restart: unless-stopped
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - DJANGO_SETTINGS_MODULE=${DJANGO_SETTINGS_MODULE:-config.settings.development}
      - DB_ENGINE=django.contrib.gis.db.backends.postgis
      - DB_NAME=beau
      - DB_USER=beau
      - DB_PASSWORD=${DB_PASSWORD:-beau_secure_password}
      - DB_HOST=postgres
      - DB_PORT=5432
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      - postgres
      - redis
      - backend
    command: celery -A config worker -l info -c 4

  # ═══════════ Celery Beat ═══════════
  celery_beat:
    build:
      context: .
      target: ${DOCKER_TARGET:-development}
    container_name: beau_celery_beat
    restart: unless-stopped
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - DJANGO_SETTINGS_MODULE=${DJANGO_SETTINGS_MODULE:-config.settings.development}
      - DB_ENGINE=django.contrib.gis.db.backends.postgis
      - DB_NAME=beau
      - DB_USER=beau
      - DB_PASSWORD=${DB_PASSWORD:-beau_secure_password}
      - DB_HOST=postgres
      - DB_PORT=5432
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      - postgres
      - redis
      - backend
    command: celery -A config beat -l info --scheduler django_celery_beat.schedulers:DatabaseScheduler

volumes:
  postgres_data:
  redis_data:
  static_volume:
  media_volume: