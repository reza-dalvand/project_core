"""
URL configuration for dashboard app (پنل ادمین اپلیکیشن)
"""
from django.urls import path

app_name = 'dashboard'

urlpatterns = [
    # بعداً view های dashboard اینجا اضافه میشن
    # path('', views.home, name='home'),
]